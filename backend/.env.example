# Copiar como .env en la raíz del repo (find_dotenv() busca hacia arriba desde el cwd).
# No poner valores reales en este archivo — solo nombres de variable.

SUPABASE_URL=
SUPABASE_KEY=
DATABASE_URL=
GEMINI_API_KEY=
